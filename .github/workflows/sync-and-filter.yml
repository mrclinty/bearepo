name: Sync and Filter

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo branch
        uses: actions/checkout@v4
        with:
          ref: repo

      - name: Download upstream index.min.json
        run: |
          curl -L \
            -o index.min.json \
            https://raw.githubusercontent.com/keiyoushi/extensions/repo/index.min.json

      - name: Install Python
        run: sudo apt-get install -y python3

      - name: Filter blacklist
        run: python3 scripts/filter-taichi.py

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.min.json
          git commit -m "Sync + filtered TaichiManga index" || echo "No changes"

      - name: Push changes
        run: git push origin repo
